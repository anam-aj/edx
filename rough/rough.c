#include <cs50.h>
#include <stdio.h>

int collatz(int number);

int main(void)
{

    printf("%s\n", winner);

}

int collatz(int number)
{
    int counter = 0;
    if (number == 1)
    {
        return counter;
    }
    else if (number % 2 == 0)
    {
        counter++;
        
    }
}
